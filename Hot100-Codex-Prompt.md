请直接开发一个供我个人使用的 Windows 桌面刷题软件，暂名「Hot100 AI Coach」。核心目标是：在独立桌面窗口里刷 LeetCode Hot 100，由 GPT 批改我的代码，并在旁边提供能持续追问的算法辅导窗口。请实际编写、运行、调试和打包项目，按下面的验收标准交付。

1. 桌面软件与技术方案
目标系统为 Windows 10/11 x64，中文界面。交付可安装的 .exe 和桌面快捷方式，安装后双击即可启动。日常使用不需要我打开浏览器、VS Code、终端或手动启动开发服务器；首次账户授权可以打开系统浏览器。
建议使用 Electron + React + TypeScript、Monaco Editor 和 SQLite。桌面界面、题库、本地运行器、AI 接入、数据存储应分模块实现。如有明显更合适的方案，简短说明后实施。依赖使用经核实的稳定版本，不要凭记忆填写版本号。
默认支持 Python 3 和 C++17，两种语言都应具备真实可用的代码模板及运行能力；其他语言预留适配接口。安装包需包含可分发的运行依赖，或提供图形化安装引导和检测，不能把解释器、编译器或 Codex 依赖留成未说明的前提。

2. 最优先实现：用我的 ChatGPT 账户接入 AI
先查阅当前官方 Codex App Server 和认证文档，再设计接入：
- [Codex App Server](https://learn.chatgpt.com/docs/app-server)
- [认证说明](https://learn.chatgpt.com/docs/auth)
优先由桌面主进程启动官方 Codex App Server，通过 stdio 通信，并提供「使用 ChatGPT 账户登录」。采用官方托管登录，让我在系统浏览器中登录现在使用的账户，凭据及刷新交给官方认证组件处理。
先做最小验证：真实登录、提交一段算法代码、收到真实批改、继续追问。记录验证结果，登录按钮或模拟回答不能算接入成功。
显示实际使用的账户、模型及可获得的额度信息；模型选择依据服务实际返回能力，不硬编码“最新模型”。处理退出登录、授权过期、额度不足、网络故障和请求取消。
这条路线使用账户可用的 Codex 权限与额度；不要承诺继承 ChatGPT 网页全部模型、记忆或当前聊天记录。不要抓取浏览器 Cookie、提取网页会话令牌或使用非官方转发服务；不要擅自覆盖我已有的全局 Codex 配置。
OpenAI API Key 可以作为可选备用入口，必须明确说明单独计费，只有我主动选择并配置后才能启用，不能在订阅额度不足时自动切换。账户路线若失败，应报告实际原因，继续完成不依赖登录的开发，保留待验证项。

3. Hot 100 题库与刷题功能
以[力扣官方 LeetCode 热题 100](https://leetcode.cn/studyplan/top-100-liked/)为目标题单，核实题目 ID、标题和分类，记录来源与核实日期，不要凭记忆随意凑满 100 题。
实现题单浏览、关键词搜索、难度/标签/完成状态筛选、上一题/下一题、随机题、收藏和进度统计。
题目页包含题目描述、示例、约束、难度、标签及对应语言的初始代码，保留原题链接；提示、题解默认折叠。
题目内容从允许使用的来源获取，支持缓存及 JSON/Markdown 导入。获取失败时明确显示缺失并提供导入入口，不能编造原题、测试数据或付费内容。最终验收统计真正能阅读、编写和运行的题目数量，只有标题或占位符不算完成。
代码编辑器支持语法高亮、行号、缩进、补全、格式化、查找替换、撤销重做、字体调节、快捷键及深浅色主题。切换题目或语言时分别保存草稿。
提供「运行测试」「提交并让 GPT 批改」「重置代码」；重置前保留可恢复版本。保存每次提交的代码、测试结果、AI 批改和时间，并支持历史查看、笔记、错题与复习标记。

4. 真实运行与判题
「运行测试」必须实际执行用户代码，支持示例和自定义用例，显示输入、预期结果、实际结果、通过数量、标准输出和错误信息。
支持 Hot 100 所需的数组、字符串、链表、二叉树等输入输出适配，以及原地修改、允许多种答案或无序输出的专用比较规则；不能统一用字符串相等代替正确判题。
补充经过校验的边界用例，必要时使用可信参考实现进行小规模对拍。GPT 生成的用例和预期答案必须标明来源并核验，不能直接当作权威答案。
运行器应与界面及认证凭据隔离，落实执行超时、停止任务、输出上限和必要的资源限制；明确实际隔离范围，不把普通子进程包装成安全沙箱。
明确区分「本地测试通过」「AI 评审结论」「力扣官方判题结果」。未获得官方结果时，不能显示官方 AC、官方执行排名或伪造隐藏测试通过。
提供打开原题和复制代码功能。力扣账户同步与官方提交另行核实支持途径；可实现就真实接入，有阻碍就说明具体缺口，不要伪造接口或同步状态。

5. GPT 批改是核心功能
点击「提交并让 GPT 批改」后，先运行测试，再将当前题目、约束、语言、提交时的代码快照和真实测试结果交给模型。
批改以中文展示：总体判断、具体错误及对应代码位置、失败原因、可复现反例、时间和空间复杂度、是否适合题目规模，以及最小必要修改建议。
不要只返回“正确/错误”，不要把测试通过当成算法正确性的证明。缺乏证据时说明不确定性，不能声称运行过实际未运行的测试。
批改必须绑定对应提交版本，后续编辑代码不能让旧批改看起来针对新代码。修改建议先展示差异，由我点击应用；默认不直接覆盖我的答案。
未连接 AI、请求失败或额度耗尽时明确展示状态，本地编辑和测试继续可用，不用固定文本冒充真实 GPT 批改。

6. 旁边的算法辅导窗口
右侧提供可调整宽度、折叠和独立弹出的聊天窗口，支持连续对话、流式回答、停止生成、重试和 Markdown/代码显示。
每道题保存独立对话；切题后切换对应记录，重启软件后可以继续。发送消息时携带当前题目、最新代码快照、选中代码及相关测试结果，让我不用反复复制粘贴。
默认进入「引导模式」：我没思路时先问我卡在哪里或已有哪些想法，一次给一个适量提示，逐步解释，而不是立即给完整答案。
提供「给一点提示」「检查我的思路」「解释选中代码」「找反例」「分析复杂度」「给完整题解」等快捷操作。仅在我要求完整题解时直接展示完整解法。
AI 默认只获得本题所需上下文；评审和聊天不能擅自修改其他文件或执行无关操作。

7. 界面与数据
界面以题单导航、题目说明、代码编辑和右侧 AI 辅导为主要区域，运行结果放在编辑区下方。区域可折叠和拖动调整，在普通笔记本分辨率及常见缩放比例下可用，聊天时仍能看到题目和代码。
草稿、笔记、提交、聊天和学习进度保存到本地用户数据目录，支持备份与导入导出，重启及升级不能清空。
登录凭据或 API Key 不写入前端代码、普通日志、数据库备份或 Git。无网络时仍能浏览已缓存题目、编辑代码、运行已安装的语言环境。

8. 开发顺序与交付验收
先检查当前工作目录和环境，简短列出架构与里程碑，然后开始实现。普通技术选择请自主处理，只有账户授权、付费选择或无法推断的关键问题需要我参与。
开发顺序：
第一步，验证 Windows 桌面窗口及真实 ChatGPT 账户接入，完成一次 AI 批改和连续追问。
第二步，完成一题的完整流程：读题、写代码、测试、GPT 批改、旁边追问、保存、重启恢复。
第三步，扩展完整 Hot 100、两种语言和不同题型适配，补齐编辑器、历史、进度和备份。
第四步，打包安装程序并验证从桌面快捷方式启动。
针对关键风险进行测试：错误代码和正确代码的判定、链表与树的序列化、特殊比较器、超时停止、真实 AI 调用、授权/网络失败、切题上下文以及重启恢复。
交付完整源码、中文 README、构建脚本、Windows 安装包、实际测试记录，以及准确的已完成/未完成清单。检查标准以可运行行为为准，不能把网页预览、静态界面、TODO 或 mock 演示称为成品。
若当前环境无法构建或验证 Windows 安装包，应完成可做的工作，提供可复现构建步骤并标明未验证项，不要声称已经在 Windows 上运行成功。
